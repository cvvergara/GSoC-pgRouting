#!/usr/bin/perl -w

%main::tests = (
    'any' => {
        'comment' => 'foobar',
        'data' => [''],
        'tests' => [qw(
            doc-foobar
            )],
        'documentation' => [qw(
            doc-foobar
            )]
    },

);

1;
